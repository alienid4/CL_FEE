# 開發與 CI 用的完整清單 ＝ 執行時相依 ＋ 測試工具。
#
# 執行時那幾個套件不寫在這裡，改用 -r 引入 requirements-runtime.txt，
# 讓它們只有一個真實來源。2026-07-21 才踩過這個坑：notebook-package 底下那份
# 複製出去的清單漏了 openpyxl，因為是各自維護的副本沒人發現，差點送出
# Excel 匯入匯出壞掉的部署包。清單有兩份就一定會漂移，所以這裡不再重寫一次。
-r requirements-runtime.txt

# ── 以下只有跑測試才需要，部署端不裝 ──────────────────────────────────
# 版本同樣用 == 鎖死（2026-07-30 使用者指示）：測試工具漂版會讓「同一份程式碼
# 在我這裡綠、在 CI 紅」，而且 pytest 大版本更新常改變 assert 行為與 fixture 語意。
# 升版流程跟執行時套件一樣：改這裡 → 跑完整測試 → 進 patch。
pytest==9.1.1
httpx==0.28.1
playwright==1.61.0
# 平行跑測試。commit 前的完整測試從 4 分半降到 1 分 45 秒（8 核實測），
# 檢查範圍不變。checks.py 在沒裝它時會自動退回序列執行。
pytest-xdist==3.8.0
