# CHANGELOG — CL_FEE 費用合約控管系統

> 這份是給人和 AI 直接讀的版本史，記「**幾月幾號幾點 · 版本 · 改了什麼 · 為什麼**」。
> **權威來源仍是 git 歷史**（`git log`，每個 commit 有完整時間戳與 diff）；本檔是摘要與導讀。
> 用途：日後若為符合公司規範需**換語言打掉重寫**，本檔 + `MVP_DONE.md` / `README_RUN.md` /
> `KNOWN_LIMITS.md` 就是「做過什麼、為什麼這樣做」的重寫依據，不用重新考古 git。
> 版本號 `vX.Y.Z` 只增不重複；同時寫進後端 `BACKEND_BUILD` 與前端 `BUILD_TAG`，畫面右上徽章顯示。

---

## v0.32.0 — 2026-07-21 13:22
- 專案搬離 OneDrive 到 C:\AiProject\CL_FEE（雲端同步會把檔案變成佔位符、弄壞 .git/index）；同步修掉 launch.json 用 python 空殼的問題與過期路徑說明

## v0.31.0 — 2026-07-21 12:05
- commit 前的完整測試改為平行執行，4 分半降到 1 分 52 秒；檢查範圍完全不變，沒裝 pytest-xdist 時自動退回序列

## v0.30.0 — 2026-07-21 11:05
- requirements.txt 補上遺漏的 openpyxl：Excel 匯入匯出是執行時相依，因寫成函式內 import 而在裝過它的機器上永遠看不出來，新部署的機器一用 Excel 就會炸

## v0.29.0 — 2026-07-21 10:24
- 修好長期紅燈的 local-ci：styles.css 斷言比對前先正規化換行符（工作區剛好是 LF 才會過，乾淨 checkout 轉 CRLF 必失敗）

## v0.28.0 — 2026-07-21 09:58
- start.bat/start.sh 改名為 demo_start.bat/demo_start.sh，檔名直接說明它是 8025 展示模式而非正式入口；文件同步更新，download.bat 不再派送展示用檔案與文件

## v0.27.0 — 2026-07-21 09:46
- download.bat 不再同步 start.bat/start.sh（它們跑的是 8025 試用模式配空的 preview.db，名字卻像主要入口，派送到部署機器等於邀請人點錯看到空系統）

## v0.26.0 — 2026-07-21 09:39
- start.bat 改用實際執行驗證的 Python 偵測（原本 where python 會找到微軟商店空殼而誤判通過，之後靜默失敗還叫人看空白的上方訊息）

## v0.25.0 — 2026-07-21 08:16
- download.bat 同時更新 service 控制台腳本與自己（原本只同步 app 與 tests，控制台的修正永遠送不到需要的機器）

## v0.24.0 — 2026-07-21 07:50
- update.bat 改名為 download.bat（upload/download 才是天然一組，方向不會記錯），舊名保留為轉接檔

## v0.23.0 — 2026-07-20 16:57
- update.bat 顯示版本前後對照並靜音無害雜訊（curl進度表與pip警告讓人誤以為更新失敗）

## v0.22.0 — 2026-07-20 16:33
- upload/service 視窗不再無聲關閉：加上未預期錯誤攔截與必定停留的提示

## v0.21.0 — 2026-07-20 16:17
- 修正 upload 寫入 CHANGELOG 的比對方式（CRLF 檔案會靜靜略過）並補回 v0.19/v0.20 版本紀錄

## v0.20.0 — 2026-07-20 16:05
- **新增 `upload.bat` 上傳工具**：上傳前列出這次改了哪些檔（已修改／新增／刪除，含檔案數與變動行數），自動把版本號同步打進四個位置（後端 BACKEND_BUILD、前端 BUILD_TAG、index.html 的兩個 cache-bust token）並寫入本檔。
- 攔截敏感檔：CL_FEE 是公開 repo，偵測到 xlsx/db/env 等檔案會要求輸入 YES 才繼續；未納入版控的新檔需個別確認，不使用 `git add -A`。
- `.gitignore` 補上交接摘要、`*.skill`、`notebook-package/app|tests|data|logs`（部署產物不該回頭進版控）。
- 為什麼：版本號散在四個地方，漏掉 cache token 會讓瀏覽器繼續載舊的 app.js，畫面看起來像沒更新——這個坑踩過不只一次，改成自動化。

## v0.19.0 — 2026-07-20 14:30
- **燈號全站統一**：線性進度圖／工作項／專案清單共用同一套語意——灰=已完成、白=未開始、綠=執行中正常、橘=快到期、紅=過期、虛線空心=不適用。判斷邏輯集中到 `ragOf()`。
- 完成從綠改灰是刻意的：做完的東西不該跟「執行中正常」搶注意力，一眼掃過去有顏色的才需要處理。
- **專案清單「燈號」欄改自動判定**：原本讀人工欄位 `rag_status`，沒人填所以整欄都是「—」。
- **完成的項目不再顯示逾期**：`pfDetail` 的「剩餘」原本只比對結束日，100% 完成的專案照樣紅字「逾期 184 天」，跟旁邊的「完成」自相矛盾。
- **催辦清單納入專案工作項**：原本只看案件／專案／合約，工作項逾期得逐案點進去才看得到。

## v0.18.0 — 2026-07-17 16:35
- **追蹤事項拆成三個獨立欄**：「風險 / 決策 / 支援」各自一欄（key: risk_note / decision_needed / support_needed），各自 inline 單格編輯、各自固定寬度(13rem)＋太長換行。
- 移除舊的合併「追蹤事項」欄與 pfItemTrackText/pfTrackHtml/cell-track（三格擠一欄的展開編輯）。
- 為什麼：三個欄位擠在同一欄裡（顯示與編輯）看起來很擠、不好讀；拆開各自成欄更清楚。
- 後端欄位不變（仍是 risk_note/decision_needed/support_needed），只改前端呈現與編輯。

## v0.17.0 — 2026-07-17 16:05
- **追蹤事項欄固定寬度 + 自動換行**：`.pf-items-table .w-track` 設 `width/max-width: 22rem; white-space: normal; overflow-wrap: anywhere`，長文字換行成多行而非撐成一超長行。
- 只針對追蹤事項這一欄，其他欄維持 `nowrap`、依內容寬度。
- 為什麼：v0.16.0 改 max-content 後，若追蹤事項塞很長的文字（風險/決策/支援），會撐成一條超長行把整表拉爆；此欄改成固定寬度、超長就換 2~5 行。

## v0.16.0 — 2026-07-17 15:38
- **工作項表格改只用內容寬度**：`.pf-items-table` 設 `width: max-content; min-width: 0`，欄位靠緊、不再被拉開留空隙。
- 為什麼：使用者反映欄位間距太浪費空間。踩到兩個坑（實測才發現）：① 全域 `table { min-width: 1300px }` 逼所有表格至少 1300px；② `width: auto` 對 `<table>` 會等同填滿容器、不是縮到內容。最終用 `width: max-content` 才真的只用內容寬度、不隨容器脹大；內容太寬時仍由 `grid-scroll`（`overflow-x:auto`）出左右捲軸。

## v0.15.0 — 2026-07-17 15:11
- **專案清單加「子項目」欄**（顯示「完成/總數」，例 1/4）。
- 後端新增 `list_projects()`：沿用 `list_rows`（保留承辦 owner scope），再一次 grouped COUNT 補 `item_count`/`item_done`；不動 schema、不 N+1。
- 為什麼：專案清單原本看不出每案底下有幾個工作項，要逐案點進去才知道。

## v0.14.0 — 2026-07-17 14:48
- 後台資料管理磚塊：**人員管理移到單位管理正後方**（兩者都是名單主檔）。
- 專案清單「進度（預計／實際）」**拆成 3 個獨立對齊欄**：進度條 / 預計% / 實際%。
- 為什麼：原本擠一欄寬度不一、看起來歪。

## v0.13.0 — 2026-07-17 14:25
- 工作項表格：**緊湊、內容置中、超寬可左右捲**（padding 縮到 4px 8px）。
- **新增流程改直覺版**：拿掉綠色「新增工作項」表單框；按＋新增＝直接在表格多一列並自動聚焦「工作主項目」inline 編。
- 燈號警告門檻：快到期由 3 天改 **14 天**（黃＝14天內快到期 或 進度落後預期 >10%）。

## v0.12.0 — 2026-07-17 14:09
- **工作項燈號改自動判定**（不再手選）：依 開始日/結束日/完成度/今天 自動算 —— 綠=完成或執行中、黃=警告、紅=過期、灰=未開始。
- **版本徽章加日期時間**（vX.Y.Z · 日期 時間），較好判斷載入的是哪一版。

## v0.11.0 — 2026-07-17 11:19 ~ 13:15
- **工作項改 Excel 式 inline 編輯**：點格子直接改、Enter/失焦自動存、Esc 取消（後端 `PATCH /api/project-items/{id}` 單欄更新）；追蹤事項點開風險/決策/支援三格。
- 13:03 對齊版號 v0.10.0→v0.11.0（前後端 build 同步）。
- 13:15 **修「死板」病灶**：bump `index.html` 靜態資源 `?v=` cache-bust token —— 之前改了 app.js/css 卻沒動 token，瀏覽器持續載快取舊檔，使用者看到的仍是舊版。

## v0.10.0 — 2026-07-12 20:01
- 匯入/匯出頁改 8 子頁籤（案件/預算/專案/簽呈/合約/請購/文件/付款）＋ Notebook 更新/服務控制/公開 patch 腳本。

## v0.9.x 及更早（2026-07-12 以前）
- pilot 試營運版基礎：FastAPI + SQLite、本機帳號雜湊 session、八大控管模組 CRUD、雙人複核、CIO 決策總覽、逾期催辦、Excel 匯入 dry-run 預覽/檢核/preflight（正式寫入仍依規則封鎖）。
- 詳細逐版變更見 `git log`（權威來源）。

## v0.20.0 — 2026-07-20 16:10
- 新增 upload.bat 上傳工具：列出變更檔案、自動打版本號、擋敏感檔

